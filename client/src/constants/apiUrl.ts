const URL_AUTH_LOGIN = '/auth/login';
const URL_AUTH_LOGOUT = '/auth/logout';
const URL_FUNDINGS = '/fundings';

export default Object.freeze({
  URL_AUTH_LOGIN,
  URL_AUTH_LOGOUT,
  URL_FUNDINGS
});
