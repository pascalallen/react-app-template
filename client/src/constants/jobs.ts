const IMPORT_STUDENTS = 'IMPORT_STUDENTS';

export const jobType = {
  IMPORT_STUDENTS
};

const STARTED = 'STARTED';
const COMPLETED = 'COMPLETED';
const FAILED = 'FAILED';

export const jobStatus = {
  STARTED,
  COMPLETED,
  FAILED
};
