const STUDENTS_IMPORTED = 'StudentsImported';
const STUDENTS_IMPORT_INVALID = 'StudentsImportInvalid';
const STUDENTS_IMPORT_FAILED = 'StudentsImportFailed';
const MAX_STUDENTS_EXCEEDED = 'MaxStudentsExceeded';

export default Object.freeze({
  STUDENTS_IMPORTED,
  STUDENTS_IMPORT_INVALID,
  STUDENTS_IMPORT_FAILED,
  MAX_STUDENTS_EXCEEDED
});
